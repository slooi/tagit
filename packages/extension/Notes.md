


// render btns
// check if should 




/* 
- code locality
- code sequence
- things that change a lot should have an interface

*/



/* 
if url domain is "x.com":
- Find mediaElements
- Filter mediaElements (overlay, within bounds of screen, not too small)
- Rendering Panels: filteredMediaElements.map(filteredMediaElement=><Panel key={filteredMediaElement} />)
- Rendering Tags: tags.map(tag=><TagButton val={tag} tag={tag}/>)
- onClick: like post, tell content/background/server to download, display saving notification, 

if domain is "pixiv.com":
- Find mediaElements
- Filter mediaElements (overlay, within bounds of screen, not too small)
- Rendering Panels: filteredMediaElements.map(filteredMediaElement=><Panel key={filteredMediaElement} />)
- Rendering Tags: tags.map(tag=><TagButton key={tag} tag={tag}/>)
- onClick: like post, tell content/background/server to download using jpg/png fallover, display saving notification, 
*/


/* 
website domain vs img domain
*/